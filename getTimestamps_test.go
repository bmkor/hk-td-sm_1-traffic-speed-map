package main

import (
	"testing"
)

func TestGetTimestamps(t *testing.T) {
	// go createLogger()
	// ts := getTimestamps("20190901", "20190902")
	// downloadTSDXML(ts)
	// start, _ := time.Parse("2006", "2017")
	// end, _ := time.Parse("2006", "2019")
	// end = end.AddDate(0, 11, 31).Add(-24 * time.Hour)
	// td := int(end.Sub(start).Hours() / 24)
	// var d [][]string
	// for i := 0; i < td; i += 5 {
	// 	s := start.AddDate(0, 0, i)
	// 	e := start.AddDate(0, 0, i+4)
	// 	if e.After(end) {
	// 		e = end
	// 	}
	// 	sf := s.Format("20060102")
	// 	ef := e.Format("20060102")
	// 	d = append(d, []string{sf, ef})
	// }
	// t.Log(d)
}
